/**
 * @file: `${PREFIX}/automation/structure.ts`
 * @description: TypeScript for creating the project deliverable directories structure
 */
import {config} from 'node-config-ts';
const structure = config.structure;

console.debug(structure);